<div class="container">
  <nav class="blog-nav hidden-xs">
    <ul class="nav">
      {{#foreach navigation}}
        <li class="nav-{{slug}}{{#if current}} active{{/if}}">
          <a href="{{url absolute="true"}}">{{label}}</a>
        </li>
      {{/foreach}}
    </ul>
  </div>
</div>
